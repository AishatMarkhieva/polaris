"""
settings.py — все настройки игры в одном месте.

Держать константы здесь — хорошая привычка: чтобы подкрутить баланс
(скорость, прыжок, гравитацию, цвета), не нужно лазить по всему коду.
"""

# ---------------------------------------------------------------------------
# ЭКРАН / РЕНДЕР
# ---------------------------------------------------------------------------
TILE = 16                       # размер одного тайла в "пиксельных" единицах
GRID_W, GRID_H = 30, 18         # сколько тайлов помещается на внутренний экран
INTERNAL_W = TILE * GRID_W      # 480 — ширина внутреннего (низкого) разрешения
INTERNAL_H = TILE * GRID_H      # 288 — высота внутреннего разрешения
SCALE = 3                       # во сколько раз увеличиваем картинку на окне
WINDOW_W = INTERNAL_W * SCALE   # итоговое окно 1440 x 864
WINDOW_H = INTERNAL_H * SCALE
FPS = 60
TITLE = "POLARIS"

# ---------------------------------------------------------------------------
# ЦВЕТА (палитра). Меняй здесь — поменяется по всей игре.
# ---------------------------------------------------------------------------
C_BG_TOP     = (18, 16, 28)
C_BG_BOTTOM  = (30, 26, 46)
C_WALL       = (70, 66, 92)
C_WALL_EDGE  = (96, 92, 124)
C_BLUE       = (70, 150, 240)     # полярность 1
C_BLUE_DARK  = (40, 90, 160)
C_ORANGE     = (245, 150, 60)     # полярность 2
C_ORANGE_DARK= (170, 95, 30)
C_SPIKE      = (220, 70, 90)
C_CRYSTAL    = (120, 240, 200)
C_EXIT       = (235, 230, 120)
C_TEXT       = (235, 235, 245)
C_TEXT_DIM   = (150, 150, 170)
C_WHITE      = (255, 255, 255)

# Удобный словарь: по имени полярности -> (яркий, тёмный) цвет
POLARITY_COLORS = {
    "blue":   (C_BLUE, C_BLUE_DARK),
    "orange": (C_ORANGE, C_ORANGE_DARK),
}

# ---------------------------------------------------------------------------
# ФОНЫ УРОВНЕЙ — свой градиент (верх, низ) на каждый уровень.
# Уровень N берёт цвет LEVEL_BACKGROUNDS[N % длины списка], поэтому даже в
# бесконечном режиме фон будет меняться по кругу.
# А ещё: если положить картинку assets/backgrounds/level_01.png (02, 03...) —
# игра возьмёт её ВМЕСТО градиента для этого уровня. Картинка важнее цветов.
# ---------------------------------------------------------------------------
LEVEL_BACKGROUNDS = [
    ((18, 16, 28), (30, 26, 46)),   # уровень 1 — фиолетовые сумерки
    ((12, 24, 34), (20, 44, 58)),   # уровень 2 — холодная синь
    ((30, 18, 22), (52, 26, 30)),   # уровень 3 — тёплый багровый
    ((14, 22, 18), (24, 40, 32)),   # уровень 4 — глубокий зелёный
]

# ---------------------------------------------------------------------------
# ФИЗИКА ИГРОКА (значения подобраны под "отзывчивый" платформер)
# Всё в пикселях за кадр / за кадр^2.
# ---------------------------------------------------------------------------
PLAYER_W, PLAYER_H = 12, 14
MOVE_ACCEL   = 0.7      # как быстро разгоняемся
MOVE_MAX     = 2.4      # максимальная горизонтальная скорость
FRICTION     = 0.55     # торможение, когда не жмём влево/вправо (0..1)
GRAVITY      = 0.32
FALL_MAX     = 6.0      # лимит скорости падения
JUMP_VELOCITY= -5.8     # сила прыжка (отрицательная = вверх)
COYOTE_TIME  = 6        # кадров "прощения" прыжка после схода с платформы
JUMP_BUFFER  = 6        # кадров буфера: нажал прыжок чуть раньше приземления

# ---------------------------------------------------------------------------
# КАМЕРА
# ---------------------------------------------------------------------------
CAMERA_LERP  = 0.12     # плавность слежения камеры (0..1, больше = резче)

# ---------------------------------------------------------------------------
# ЛЕГЕНДА ТАЙЛОВ (символы в файлах уровней levels/*.txt)
# ---------------------------------------------------------------------------
#   .  пусто
#   #  стена (всегда твёрдая, нейтральная)
#   B  синий блок   (твёрдый, когда игрок СИНИЙ)
#   O  оранжевый блок (твёрдый, когда игрок ОРАНЖЕВЫЙ)
#   ^  шипы (опасность — смерть)
#   *  кристалл (собрать)
#   @  старт игрока
#   E  выход (завершить уровень)
#
# Чтобы добавить новый тип тайла — допиши его сюда и обработай в level.py.
